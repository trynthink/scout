name: EIA update check

on:
  push:
    branches: 
       - master
       - issue-517/eia_update_helpers
  workflow_dispatch: {}

jobs:
  eia-check:
    runs-on: ubuntu-latest

    env:
      # EIA API key stored as a GitHub Actions secret
      EIA_API_KEY: ${{ secrets.EIA_API_KEY }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install package
        run: |
          python -m pip install --upgrade pip
          python -m pip install ".[dev]"

      - name: Run EIA baseline comparison (save output)
        run: |
          mkdir -p tests/eia_update
          python -m scout.AEO_update_helpers.baseline_comparison --year 2025 | tee tests/eia_update/eia_baseline_output.txt

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: eia-baseline-results
          path: |
            ./tests/eia_update/
            ./generated/log_*.txt
